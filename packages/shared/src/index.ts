/**
 * @1manbiz/shared
 *
 * Shared TypeScript types and utilities consumed by apps/web and apps/mobile.
 * Populate incrementally as cross-app patterns emerge (Supabase typegen,
 * domain types, validators, formatters).
 */

export {};
